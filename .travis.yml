sudo: required

services:
    -docker

before_install:
    -docker build -t estefaduque-webapp -f Dockerfile.test .

script:
    -docker run -e CI=true estefaduque-webapp-test

before-deploy:
    -docker  build -t estefaduque-webapp .

deploy:
    skip_cleanup: true
    provider: script
    script: bash ./deploy.sh
    on:
        branch: master
